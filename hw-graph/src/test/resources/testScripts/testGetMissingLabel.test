# Tests the method getLabel in DLGraph by adding nodes
# and edges to a graph and checking if the label returned
# is correct
CreateGraph A
AddNode A one
AddNode A two
AddNode A three
AddEdge A one two e12
AddEdge A one three e13
GetLabel A two three
GetLabel A one Two
